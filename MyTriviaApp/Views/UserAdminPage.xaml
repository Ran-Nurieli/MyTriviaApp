<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyTriviaApp.Views.UserAdminPage"
             Title="UserAdminPage">
    <VerticalStackLayout >
        
        <Label Text="User Management" FontSize="Large" HorizontalOptions="Center" />
        <HorizontalStackLayout HorizontalOptions="Center">
            
            <Picker >
                <Picker.ItemsSource>
                    <x:Array Type="{x:Type x:String}">
                        <x:String>
                            Rookie 
                        </x:String>
                        <x:String>
                            Master
                        </x:String>
                        <x:String>
                            Admin
                        </x:String>
                    </x:Array>

                </Picker.ItemsSource>
            </Picker>
                <Button Command="{Binding LoginCommand}" Text="Filter"/>
            <Button Command="{Binding LoginCommand}" Text="Reset" BackgroundColor="Red"/>
        </HorizontalStackLayout>
        <ScrollView MaximumHeightRequest="200">

            <StackLayout>
                
                <SwipeView>
                    <SwipeView.LeftItems>
                        <SwipeItem Text="Delete" BackgroundColor="Red"/>
                        <SwipeItem Text="ResetPoints" BackgroundColor="Green"/>
                    </SwipeView.LeftItems>
                    <Grid BackgroundColor="LightGray" HeightRequest="60" WidthRequest="300">
                        <Label Text="Username" HorizontalOptions="Center" VerticalOptions="Center"/>
                    </Grid>
                </SwipeView>        
                <SwipeView>
                    <SwipeView.LeftItems>
                        <SwipeItem Text="Delete" BackgroundColor="Red"/>
                        <SwipeItem Text="ResetPoints" BackgroundColor="Green"/>
                    </SwipeView.LeftItems>
                    <Grid BackgroundColor="LightGray" HeightRequest="60" WidthRequest="300">
                        <Label Text="Username" HorizontalOptions="Center" VerticalOptions="Center"/>
                    </Grid>
                </SwipeView>     
                <SwipeView>
                    <SwipeView.LeftItems>
                        <SwipeItem Text="Delete" BackgroundColor="Red"/>
                        <SwipeItem Text="ResetPoints" BackgroundColor="Green"/>
                    </SwipeView.LeftItems>
                    <Grid BackgroundColor="LightGray" HeightRequest="60" WidthRequest="300">
                        <Label Text="Username" HorizontalOptions="Center" VerticalOptions="Center"/>
                    </Grid>
                </SwipeView>
                <SwipeView>
                    <SwipeView.LeftItems>
                        <SwipeItem Text="Delete" BackgroundColor="Red"/>
                        <SwipeItem Text="ResetPoints" BackgroundColor="Green"/>
                    </SwipeView.LeftItems>
                    <Grid BackgroundColor="LightGray" HeightRequest="60" WidthRequest="300">
                        <Label Text="Username" HorizontalOptions="Center" VerticalOptions="Center"/>
                    </Grid>
                </SwipeView>
                <SwipeView>
                    <SwipeView.LeftItems>
                        <SwipeItem Text="Delete" BackgroundColor="Red"/>
                        <SwipeItem Text="ResetPoints" BackgroundColor="Green"/>
                    </SwipeView.LeftItems>
                    <Grid BackgroundColor="LightGray" HeightRequest="60" WidthRequest="300">
                        <Label Text="Username" HorizontalOptions="Center" VerticalOptions="Center"/>
                    </Grid>
                </SwipeView>           
                <SwipeView>
                    <SwipeView.LeftItems>
                        <SwipeItem Text="Delete" BackgroundColor="Red"/>
                        <SwipeItem Text="ResetPoints" BackgroundColor="Green"/>
                    </SwipeView.LeftItems>
                    <Grid BackgroundColor="LightGray" HeightRequest="60" WidthRequest="300">
                        <Label Text="Username" HorizontalOptions="Center" VerticalOptions="Center"/>
                    </Grid>
                </SwipeView>

            </StackLayout>

        </ScrollView>



    </VerticalStackLayout>
</ContentPage>